// API base URL configuration
// Production (nginx same-origin proxy): leave as ''
// Local dev (java on :8080, frontend served separately): set to 'http://localhost:8080'
window.APP_CONFIG = {
  apiBase: '',
};
