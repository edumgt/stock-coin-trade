(async()=>{const user=await initPage({requireAuth:true});if(!user)return;const q=id=>document.getElementById(id);const mark=(id,ok,text)=>{const e=q(id);if(!e)return;e.className=`kb-status ${ok?'ok':'bad'}`;e.textContent=`${ok?'✓':'!'} ${text}`};try{const response=await apiFetch('/api/broker-test/kb/status');const data=await response.json();if(!response.ok||!data.ok)throw new Error(data.message||'설정 상태를 확인하지 못했습니다.');const s=data.status;mark('kbConfigured',s.configured,s.configured?'Key·Secret 준비':'Key·Secret 필요');mark('kbSource',s.configured,'원본: .env');mark('kbMount',s.configured,'.env 단일 설정');mark('kbPermission',s.configured,'브라우저 비노출');q('kbSetupMessage').className=s.configured?'kb-note':'kb-warn';q('kbSetupMessage').textContent=s.configured?'KB API 테스트를 실행할 준비가 되었습니다.':'.env에 KB_APP_KEY와 KB_APP_SECRET을 설정하세요.'}catch(error){q('kbSetupMessage').className='kb-warn';q('kbSetupMessage').textContent=error.message}})();
